if messages.errors
  .sixteen.wide.column
    .ui.negative.message
      i.close.icon
      for error in messages.errors
        div= error.msg
if messages.info
  .sixteen.wide.column
    .ui.warning.message
      i.close.icon
      for info in messages.info
        div= info.msg
if messages.success
  .sixteen.wide.column
    .ui.positive.message
      i.close.icon
      for success in messages.success
        div= success.msg
if messages.final
  .sixteen.wide.column
    .ui.info.message
      .content
        .header
          | You made it 🎉🎉 
          p Now we want to ask you some additional questions.  
          if messages.final[0].msg
              a(href=messages.final[0].msg)  Click to follow link